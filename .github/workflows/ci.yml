name: My CI workflow

on:
    push:

jobs:
    hello:
        name: My Hello job
        runs-on: ubuntu-latest
        steps:
            - name: Print Hello
              run: echo "Hello World"